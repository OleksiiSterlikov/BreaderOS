version: "3.9"

services:
  bookreader:
    container_name: breaderos
    build: .
    ports:
      - "5000:5000"
    volumes:
      # 🔥 книги зберігаються поза контейнером
      - ./static/books:/app/static/books
    restart: unless-stopped
