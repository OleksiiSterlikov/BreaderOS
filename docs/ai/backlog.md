# BACKLOG.md

Цей файл використовується як постійний реєстр робіт по проєкту `BreaderOS`.

Позначення статусу:

- `[ ]` не виконано
- `[+]` в процесі
- `[x]` виконано

## Першочергові заходи

- [x] Закрити path traversal у `/api/folder`: обмежити доступ тільки директорією `static/books` через перевірку `realpath/commonpath`.
- [x] Прибрати дубльований маршрут `/book/<path:...>` і залишити один безпечний обробник віддачі файлів.
- [x] Синхронізувати `docs/ai/SECURITY.md` з реальним станом проєкту після виправлення безпеки.
- [x] Синхронізувати `docs/ai/DEPLOYMENT.md` з фактичним деплоєм у репозиторії або реалізувати описану інфраструктуру `Gunicorn + Nginx`.
- [x] Синхронізувати `docs/ai/ARCHITECTURE.md` з фактичною архітектурою застосунку.

## Другорядні

- [x] Оновити `README.md`: додати реальні інструкції інсталяції та локального запуску.
- [x] Оновити `README.md`: прибрати або уточнити згадку про `Content Manager`, якщо такого компонента в проєкті немає.
- [x] Оновити `README.md`: скоригувати опис функції zoom, бо зараз реалізовано зміну масштабу шрифту, а не окремий zoom для зображень.
- [x] Прибрати `print("PAGES:", pages)` з головного маршруту або замінити на нормальне логування.
- [x] Уточнити в документації, що lazy loading використовується для дерева папок, але список HTML-сторінок для prev/next формується повним обходом файлової системи.
- [x] Привести `docs/ai/DEVELOPMENT.md` у відповідність до фактичного середовища запуску, залежностей і команд.
- [x] Стабілізувати локальний/dev запуск: винести створення app у `create_app()` і зробити запуск керованим через env-параметри.

## У майбутньому

- [ ] Довести production/dev runtime-конфігурацію до експлуатаційного рівня: окремі профілі запуску, керовані env-параметри, документований workflow для `Flask` і `Gunicorn`.
- [x] Додати базові автоматизовані тести для маршрутів `/`, `/api/folder` і `/book/<path>`.
- [x] Додати перевірку документації на актуальність під час релізів або перед деплоєм.
- [ ] Продумати окремий механізм імпорту або адміністрування книг, якщо `Content Manager` має стати реальною частиною системи.
- [ ] Оптимізувати побудову списку `BookPages`, щоб не робити повний обхід усіх HTML-файлів на кожен запит головної сторінки.
